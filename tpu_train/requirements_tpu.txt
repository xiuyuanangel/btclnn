# ================================================================
# LNN TPU 训练依赖 — 在 Google Cloud TPU VM 上安装
# ================================================================
# 在 TPU VM 上，torch 和 torch-xla 需通过 pip 指定版本安装：
#   pip install torch~=2.4.0 torch-xla[tpu]~=2.4.0 \
#       -f https://storage.googleapis.com/libtpu-releases/index.html
#
# Google Cloud TPU VM 预装了大部分依赖，只需补充本项目特有依赖。
#
# 安装命令 (TPU VM):
#   pip install -r requirements_tpu.txt
# ================================================================

# ---------- 核心 (已在 TPU VM 上预装，此处列出版本约束) ----------
torch>=2.4.0
torch-xla>=2.4.0

# ---------- 数据科学 ----------
numpy>=1.24.0
pandas>=2.0.0

# ---------- 网络 ----------
requests>=2.28.0

# ---------- 工具 ----------
psutil>=5.9.0

# ---------- 可选 (通知推送) ----------
# httpx>=0.27.0
